name: Idea or request
description: Something the plugin should do that it does not.
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        This plugin is deliberately small, and staying small is a feature of it. That does not mean no — it
        means the case has to be made. The most persuasive thing you can write here is what you are actually
        trying to do.
  - type: textarea
    id: problem
    attributes:
      label: What are you trying to do?
      description: The situation, not the solution. What are you doing today instead?
    validations:
      required: true
  - type: textarea
    id: idea
    attributes:
      label: What did you have in mind?
      description: If you have a shape in mind, describe it. If not, that is fine — the problem is the useful half.
  - type: dropdown
    id: area
    attributes:
      label: Which part would it touch?
      options:
        - Forms (block or shortcode)
        - Pop-ups
        - Audience (the opt-in box)
        - Site email
        - The settings screen itself
        - Not sure
  - type: checkboxes
    id: checks
    attributes:
      label: Before you post
      options:
        - label: I have checked the [documentation](https://sendbeam.io/docs/wordpress) in case it already does this.
        - label: I have searched the existing issues.
